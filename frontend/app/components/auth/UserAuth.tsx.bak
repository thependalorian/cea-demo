'use client'

import React from 'react'


interface UserAuthProps {
  children: React.ReactNode
  fallback?: React.ReactNode
}

export default function UserAuth({ children }: UserAuthProps) {

  // In demo mode, always allow access to user features
  return <>{children}</>
} 